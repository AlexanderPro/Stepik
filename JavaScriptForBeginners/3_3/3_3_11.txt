¬опрос:
¬ этом задании в нашу функцию testRegExp первым параметром передаетс€ случайна€ строка(переменна€ s), а вторым - случайна€ подстрока(переменна€ sub_s), которую нужно использовать в качестве шаблона регул€рного выражени€. ¬ам нужно вернуть из функции строку, в которой будут перечислены через зап€тую все совпадени€ шаблона с первой строкой.

ќтвет:
function testRegExp(s, sub_s) {
    const regExp = new RegExp(sub_s, 'g');
    const result = [];
    let finded_s = null;
    while ((finded_s = regExp.exec(s)) != null) {
        result.push(finded_s);
    }
    return result.join(',');
}