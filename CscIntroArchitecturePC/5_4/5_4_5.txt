Вопрос:
Приложению требуется открыть и прочитать в память файл, хранимый на диске. Сколько блоков потребуется считать с диска для выполнения этой операции, если размер файла 2347722 байта, а диск разбит на блоки по 4096 байт.

Ответ:
574