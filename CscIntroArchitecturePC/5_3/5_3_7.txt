Вопрос:
В предшествующем видеофрагменте допущена неточность в отношении памяти для хранения переменной p в функции g(). В действительности, тут встречается 2 вида памяти:

память, для хранения указателя p в стеке функции g()
память для хранения 10 байт, выделенной оператором new char[10] в системной куче
С учетом этого замечания, выберите корректное значение, которое потребуется выделить для переменной d в стеке, при вызове следующей функции, если размер указателя равен 4 байта:


func()
{
 char *d = new char[798];
}

Ответ:
4