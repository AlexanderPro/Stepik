Вопрос:
Вычислите размер стекового кадра в байтах для вызова следующей функции, если в рассматриваемой системе размер памяти для хранения указателей, целых чисел (int) и чисел с плавающей точкой (float) равен 4 байта.


f(int a, float b)
{
 float t;
 int d;
 // полезные вычисления 
}

Ответ:
20